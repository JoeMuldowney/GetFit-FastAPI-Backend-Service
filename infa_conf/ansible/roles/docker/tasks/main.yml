- name: Install Docker
  apt:
    name:
      - docker.io
      - docker-compose-v2
    state: present

- name: Enable and start Docker
  systemd:
    name: docker
    enabled: true
    state: started